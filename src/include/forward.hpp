#ifndef FORWARD_HPP
#define FORWARD_HPP

namespace parser {
    class cfg;
    class cfg_builder;
    class LR_graph;
    class cfg_reduction;

    template<typename DataType, typename Trans>
    class graph;

    template<typename DataType, typename Trans>
    class connection;
}

#endif
